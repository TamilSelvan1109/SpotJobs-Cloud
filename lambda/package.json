{
  "name": "resume-scoring-lambda",
  "version": "1.0.0",
  "description": "AWS Lambda function for resume scoring with Textract",
  "main": "resumeScoring.js",
  "dependencies": {
    "@aws-sdk/client-textract": "^3.0.0",
    "axios": "^1.6.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}